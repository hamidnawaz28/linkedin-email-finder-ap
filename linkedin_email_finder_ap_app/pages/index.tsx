import React from "react";
import { Subscription, Header, Footer } from "@components";

const index = () => {
    return (
        <div>
            <Header />
            <Subscription />
            <Footer />
        </div>
    );
};

export default index;
